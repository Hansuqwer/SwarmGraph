# hive-swarm

LangGraph swarm runtime: queen/worker decomposition, consensus protocols, HITL approval, anti-drift, streaming guards, and audit signing.

```bash
pip install hive-swarm
```

Default mode is stub-only and requires no API key.

Run package tests:

```bash
pytest packages/hive-swarm/tests
```
