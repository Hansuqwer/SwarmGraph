# ADR 0001: Monorepo Structure

Status: Accepted

Decision: Use one GitHub repository with three package directories under `packages/`.

Rationale: shared tests, shared docs, atomic changes across `swarm-shared`, `hive-swarm`, and `ai-provider-swarm-gateway`.
