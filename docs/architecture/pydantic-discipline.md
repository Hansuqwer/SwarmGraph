# Pydantic Discipline

Core state is modeled with Pydantic v2. Models validate boundaries, cap unbounded lists, and serialize cleanly across LangGraph node boundaries.

Defaults are backward-compatible. Network execution is opt-in.
