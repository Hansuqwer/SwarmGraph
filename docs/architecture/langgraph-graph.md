# LangGraph Graph

`hive-swarm` uses LangGraph conditional routing and `Send` fan-out for worker execution.

Queen nodes decompose objectives, worker nodes execute tasks, collect nodes create votes, and consensus nodes decide the swarm output.
