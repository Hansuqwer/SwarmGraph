# Deployment

Run stub mode locally by default. For live providers, set provider-specific API key env vars and select `--backend gateway`.

Do not bake provider keys into container images or config files.
