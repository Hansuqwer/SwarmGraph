# Cost Tracking

Cost tracking is enabled by default. Disable with:

```bash
HIVE_SWARM_COST_TRACKING=0
```

Costs are estimates from `swarm_shared.pricing` and are stored in `WorkerResult.usage.cost_usd`.
